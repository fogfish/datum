{erl_opts, []}.

{deps, [
   %% Note: use hex package for productions, git master unlock recent features
   {datum, ".*",
      {git, "https://github.com/fogfish/datum.git", {branch, master}}
   }
]}.

